<div class="blank" style="padding-top: 75px;">
</div>
<div id="content" class="site-content">
  <div id="main">
    <header class="page-header">
      <h1 class="cat-title">
        归档</h1>
      <span class="cat-des">
        <p>
          Archives</p>
      </span>
    </header>
    <div id="main-part">
      <article class="art">
        <div class="art-main">
          <div class="art-content">
            <div id="archives">
              <div class="toggle-container">
                <div class="toggle-checky toggle-blue toggle-slide-up">
                  <span class="toggle-figure"></span>
                  <span class="toggle-label noselect">Show</span>
                  <span class="toggle-label noselect toggle-label-selected">Hide</span>
                </div>
              </div>
              <% var lastYear; var lastMonth; %>
              <% site.posts.sort('date', theme.homePageSortType).each(function(post, i){ %>
              <% var currentYear = parseInt(date(post.date, 'YYYY')); %>
              <% var currentMonth = parseInt(date(post.date, 'YYYYMM')); %>
              <% if (lastYear != currentYear){ %>
              <% lastYear = currentYear; %>
              <h3 class="al_year"><%= date(post.date, 'YYYY') %>年</h3>
              <% } %>
              <% if (lastMonth != currentMonth){ %>
              <% lastMonth = currentMonth; %>
              <ul class="al_mon_list">
                <li class="al_li">
                  <span class="al_mon" style="cursor: pointer;"><span class="mounth"><%= date(post.date, 'MM') %>月(<span
                        id="post-num">3</span>篇文章)</span></span>
                  <ul class="al_post_list">
                    <% } %>
                    <li>
                      <a href="<%- url_for(post.path) %>">
                        <span style="color:#0bf;">
                          <%= date(post.date, 'DD') %>日<%= time(post.date) %></span>
                        《<%= post.title %>》
                      </a>
                    </li>
                    <% if (!site.posts.sort('date', theme.homePageSortType).eq(i + 1) || currentMonth != parseInt(date(site.posts.sort('date', theme.homePageSortType).eq(i + 1).date, 'YYYYMM'))){ %>
                  </ul>
                </li>
              </ul>
              <% } %>
              <% }) %>
            </div>
          </div>
        </div>
      </article>
    </div>
  </div>
</div>